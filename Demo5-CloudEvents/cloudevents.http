### Programmatic Subscription
@senderUrl=http://localhost:5233
### Send
POST {{senderUrl}}/send
Content-Type: application/json

{
    "id": "{{$guid}}",
    "timeStamp": "{{$datetime iso8601}}"
}

### Send with metadata
POST {{senderUrl}}/sendwithmetadata
Content-Type: application/json

{
    "id": "{{$guid}}",
    "timeStamp": "{{$datetime iso8601}}"
}